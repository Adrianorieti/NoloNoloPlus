import * as React from 'react';
declare type RenderDayContent = (date: Date) => React.ReactNode;
declare type RenderDayLabel = ({ date, isSelected, isSelectable }: RenderDayLabelParams) => string;
interface RenderDayLabelParams {
    date: Date;
    isSelected: boolean;
    isSelectable: boolean;
}
interface Props extends React.HTMLProps<HTMLTableElement> {
    renderDayLabel?: RenderDayLabel;
    renderDayContent?: RenderDayContent;
}
declare const DatePickerCalendar: ({ renderDayLabel, renderDayContent, ...props }: Props) => JSX.Element;
export default DatePickerCalendar;
